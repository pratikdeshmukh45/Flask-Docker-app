# Flask + Node.js Dockerized Application

This project demonstrates a **multi-container application** using:

- 🟢 Node.js (Express) → Frontend
- 🔵 Flask → Backend API
- 🐳 Docker & Docker Compose → Containerization & orchestration

The frontend collects user input and sends it to the Flask backend for processing.

---

## 📂 Project Structure

